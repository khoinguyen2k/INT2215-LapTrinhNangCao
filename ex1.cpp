#include <iostream>
#include <cmath>
#include <iomanip>

#define ll long long
using namespace std;

bool is_square(ll n)
{
   return (n ==(ll)sqrt(n) *(ll)sqrt(n) );
}
int main()
{
   ll a, b;
   cin >>a >>b;

   return 0;
}
